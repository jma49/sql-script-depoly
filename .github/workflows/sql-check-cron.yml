name: SQL Script Cron Job

on:
  schedule:
    - cron: "0 19 * * *" # 每天 UTC 7 PM
  workflow_dispatch:

env:
  DATABASE_URL: ${{ secrets.DATABASE_URL }}
  SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

jobs:
  run-sql-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Create environment file
        run: |
          echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" >> .env.local
          echo "SLACK_WEBHOOK_URL=${{ secrets.SLACK_WEBHOOK_URL }}" >> .env.local
          echo "MONGODB_URI=${{ secrets.MONGODB_URI }}" >> .env.local
          echo "NODE_ENV=production" >> .env.local

      - name: Debug directory structure
        run: |
          pwd
          ls -la
          ls -la scripts/
          ls -la scripts/sql_scripts/
          echo "--- .env.local content start ---"
          cat .env.local || echo ".env.local not found or empty"
          echo "--- .env.local content end ---"

      - name: Run SQL Script
        env:
          DOTENV_CONFIG_PATH: .env.local 
        run: |
          echo "当前工作目录: $(pwd)"
          echo "将使用 .env 文件: ${DOTENV_CONFIG_PATH}"
          ls -la ${DOTENV_CONFIG_PATH}
          echo "即将执行 ts-node..."
          # Use ts-node with -r to preload dotenv/config
          npx ts-node -r dotenv/config scripts/run_sql.ts check_square_order_duplicates
        continue-on-error: false
